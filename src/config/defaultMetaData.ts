// src/config/defaultMetadata.ts
import { APP_NAME, APP_DESCRIPTION, APP_URL, APP_IMAGE, APP_THEME, APP_TYPE, APP_LOCALE } from "@/config/constants/index";

export const DEFAULT_METADATA = {
  title: APP_NAME,
  description: APP_DESCRIPTION,
  siteName: APP_NAME,
  url: APP_URL,
  image: APP_IMAGE,
  theme: APP_THEME,
  type: APP_TYPE,
  locale: APP_LOCALE
};